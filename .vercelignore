# App desktop (Electron) — não faz parte do build da Vercel.
# Com barra inicial: só a pasta `desktop/` da raiz. Sem a barra, o padrão
# casava também com `app/api/desktop/` e derrubava aquela rota do deploy.
/desktop
